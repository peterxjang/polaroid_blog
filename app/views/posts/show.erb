<h2><%= @post.title %></h2><a href="/posts/<%= @post.id %>/edit">Edit this post</a>
<img class="img-fullwidth"src='<%= @post.image.url %>'>
<p>Tags:
  <% @tags.each do |tag| %>
  <a href='/tags/<%= tag.id %>'><%= tag.text %></a>
  <% end %>
</p>
<p><%= @post.body %></p>
